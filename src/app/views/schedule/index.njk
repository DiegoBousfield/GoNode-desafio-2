{% extends "_layouts/default.njk" %}

{% block body %}
  <div class="content">
    <strong>Meus Agendamentos</strong>
    <p>Veja abaixo a lista de agendamentos que você tem para hoje:</p>
    <ul class="providers">
      {%for appointment in appointments %}
        <li class="provider">
          <div>
            <img src="/files/{{ appointment.user.avatar }}">
            <strong>{{ appointment.user.name }} <br> {{ appointment.date | date("HH:mm") }}</strong>
          </div>
        </li>
      {% endfor %}

    <a class="logout" href="/app/dashboard">Voltar</a>
    </ul>
  </div>




{% endblock %}
